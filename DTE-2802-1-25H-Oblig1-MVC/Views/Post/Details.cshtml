@model DTE_2802_1_25H_Oblig1_MVC.Models.Post

@{
    ViewData["Title"] = "Details";
}
@* THIS IS A POST VIEW> *@
<h1>Details</h1>

<div>
    <h4>Post</h4>
    <hr />
    <dl class="row">

        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Title)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Title)
        </dd>

        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Content)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Content)
        </dd>

        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.BlogId)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.BlogId)
        </dd>

        <dt class="col-sm-2">
          @Html.DisplayNameFor(model => model.OwnerId)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.OwnerId)
        </dd>
    </dl>
</div>

<div>
    @* <h4>Add a Comment</h4> *@
    <form asp-controller="Comment" asp-action="Create" method="post">
        <input type="hidden" name="PostId" value="@Model.Id" />
        <div class="form-group">
            <hr />
            <label for="Content">Content</label>
            <textarea name="Content" class="form-control" required></textarea>
        </div>
        <button type="submit" class="btn btn-primary">Add Comment</button>
    </form>
</div>

<hr />
<div>
    <h4>Comments</h4>
    @if (Model.Comments != null && Model.Comments.Any())
    {
        <table class="table">
            <thead>
                <tr>
                    <th>Content</th>
                    <th>Owner</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var comment in Model.Comments)
                {
                    <tr>
                        <td>@comment.Content</td>
                        <td>@comment.OwnerId</td>
                        <td>
              @* <a asp-controller="Comment" asp-action="Edit" asp-route-id="@comment.Id">Details</a> | *@
              <a asp-controller="Comment" asp-action="Details" asp-route-id="@comment.Id">Details</a> |
              <a asp-controller="Comment" asp-action="Delete" asp-route-id="@comment.Id">Delete</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }
    else
    {
        <p>No comments for this post.</p>
    }
</div>

@* <div>
    <h4>Comments</h4>
    @if (Model.Comments != null && Model.Comments.Any())
    {
        <table class="table">
            <thead>
                <tr>
                    <th>Content</th>
                    <th>Owner</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var comment in Model.Comments)
                {
                    <tr>
                        <td>@comment.Content</td>
                        <td>@comment.OwnerId</td>
                    </tr>
                }
            </tbody>
        </table>
    }
    else
    {
        <p>No comments for this post.</p>
    }
</div> *@

<div>
  @* Hide Edit/Delete links from non-owners
     commented out to show ownership checks *@
  @*   @if (Model.OwnerId == User?.Identity?.Name)
    { <a asp-action="Edit" asp-route-id="@Model.Id">Edit</a> }
   *@  
  <a asp-action="Edit" asp-route-id="@Model.Id">Edit</a> 
  |
  <a asp-action="Index">Back to List</a>
</div>